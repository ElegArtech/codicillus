# ADR-002 — Le socle est la source unique du système visuel

- Statut : **acceptée, amendée le 18 août 2026** — la décision est inchangée, la source qu'elle désignait était fausse
- Source : `cadrage/STACK-TECHNIQUE.md` §1 (« ce que les maquettes ajoutent ») et §4.1 ; RG-DA-01 ; `cadrage/PLAN-DE-REALISATION.md` §3.3, §3.4
- Amendement : ARB-006 · `docs/errata-cadrage.md` E-01 et E-02 · `docs/ecarts/ECART-007.md`

> **Amendement — quelle est la source.** La première rédaction de cet ADR désignait
> `mockups/socle.css` et annonçait 61 jetons, reprenant le cadrage. Les deux points sont faux.
> `mockups/socle.css` est le **plus ancien de six états** du socle en ligne, employé par 4 vues
> sur 41 ; il lui manque les champs de saisie — que 21 vues emploient —, les notifications à
> quatre types, la règle de rôle `.si-admin` et le jeton `--l-large`. Le socle en compte **69**
> (70 avec `--l-large`), pas 61.
>
> **La décision elle-même ne change pas d'un iota** : source unique, jetons nommés, inventaire
> fermé, aucune classe utilitaire, aucune bibliothèque de composants. Seul l'objet désigné change.
> La source est le socle en ligne le plus complet, celui de
> `mockups/V-07-accueil-contributeur.html` (lignes 8 à 472), extrait mécaniquement par
> `verif/extraire-socle.mjs` vers `src/socle.css` — jamais recopié à la main. L'extraction est
> ancrée au gel : elle refuse de s'exécuter si l'empreinte de V-07 diverge de `mockups/GEL.md`.
>
> Le corps ci-dessous est amendé en conséquence. Cet ADR n'est pas *superseded* : il n'y a pas eu
> de changement de décision, il y a eu une erreur de fait dans le cadrage, corrigée.

## Contexte

Les 41 maquettes sont du HTML sémantique, du CSS à jetons nommés et du JavaScript natif. Vérification faite : les seules ressources externes des fichiers de vue sont trois familles typographiques. Le socle définit **69 jetons nommés** — encres, surfaces, traits, accent, fraîcheur, sémantique, typographie, échelle, espacement, rayons, élévations, dimensions de structure, mouvement — et **onze familles de composants**, dont 255 classes recensées dans `docs/DESIGN.md`.

L'ordre de préséance du projet place les maquettes au-dessus de tout : *le web design décide, la pile s'adapte*. Aucune brique de la pile n'impose de convention de nommage, de classe utilitaire, de grille ou de composant préfabriqué. Ce critère a éliminé plusieurs candidates par ailleurs excellentes.

Le risque nommé RA-02 du plan est explicite : la dérive du système visuel — valeurs en dur, composant inventé — fait perdre à la charte son caractère systémique, et c'est une perte silencieuse.

## Décision

Le **socle en ligne de `mockups/V-07-accueil-contributeur.html`** (lignes 8 à 472) est la source unique de vérité du système visuel. `src/socle.css` en est une **copie générée**, produite par `verif/extraire-socle.mjs` et **identique à l'octet**, dont la non-divergence est vérifiée mécaniquement par `pnpm verif:jetons` (contrôle P-6.1). Elle n'est jamais éditée à la main : elle se régénère.

`mockups/` est lu, jamais édité. Le refus est **bloquant, pas déclaratif** : les règles de `.claude/settings.json` ne couvrent que les outils Edit et Write, et Bash passe à travers (`ECART-004`) — la protection réelle est le retrait du bit d'écriture sur les trois dossiers de sources, doublé du contrôle d'empreintes `pnpm verif:gel`.

`docs/DESIGN.md` en dérive et publie les 69 jetons avec leur rôle, l'**inventaire fermé des composants** — onze familles, 255 classes, chacune tracée à sa ligne dans le socle ou dans `V-41` — et les règles de layout. Fermé signifie : un composant absent de cet inventaire n'existe pas.

## Ce que cette décision interdit activement

- **Toute valeur de couleur, d'espacement, de rayon, d'ombre, de police ou de graduation typographique écrite en dur** dans un composant, une feuille de vue, un attribut `style` ou un fichier de configuration. Une valeur qui n'est pas un jeton de `socle.css` est un écart, y compris si elle est numériquement identique au jeton.
- **Toute bibliothèque de composants** — Material, Bootstrap, shadcn, Skeleton, DaisyUI ou autre. Elles importent un système visuel concurrent.
- **Toute classe utilitaire** et tout générateur de classes (Tailwind et assimilés). Le CSS reste du CSS.
- **Tout composant inventé** : une famille de composants absente de l'inventaire fermé de `docs/DESIGN.md` ne se crée pas en cours de tâche. Le besoin remonte comme entrée ÉCART, l'inventaire est amendé par arbitrage, et alors seulement le composant existe.
- **Toute modification d'un fichier de `mockups/`**, y compris « pour corriger une coquille ». Le regel passe par un arbitrage humain tracé dans `mockups/GEL.md`.
- **Toute édition manuelle de `src/socle.css`.** Le fichier est généré ; l'éditer, c'est créer une divergence que P-6.1 détectera et que la régénération écrasera. Une correction du socle se fait à la source, donc par regel arbitré.
- **Tout emploi de `mockups/socle.css` comme source.** C'est un état périmé conservé au gel pour la traçabilité, pas une référence d'implémentation (E-01).
- **Toute divergence de la copie applicative** par rapport au gel, quelle qu'en soit la justification technique.

Cas particulier explicitement couvert : le **témoin de fraîcheur** est une jauge à trois barres dont **la forme porte l'information et la couleur ne fait que la répéter** (RG-M18-09, RG-DA-03). C'est le composant le plus reproduit du produit et le plus facile à dégrader. Une réimplémentation locale, une variante de couleur seule, ou un badge textuel de substitution sont interdits.

## Conséquences

- Les trois familles typographiques doivent être **rapatriées localement** : RG-NF-08 exige un produit auto-hébergeable sans dépendance externe, et les maquettes chargent aujourd'hui Google Fonts (risque RA-12). Le harnais de capture réécrit la source des polices dans la maquette au moment du rendu, faute de quoi la comparaison mesure une différence de fonderie et non une différence d'implémentation.
- L'interface se produit par **assemblage contraint** : l'agent ne dessine plus, il compose.
- Un hook de contrôle des jetons s'exécute à chaque écriture de fichier — la contrainte s'applique à la frappe, pas au rendu.
- `V-41` étant une page réelle, toute dérive y devient visible immédiatement.

## Comment le vérifier

- **Batterie 2** — `pnpm verif:jetons` : aucune valeur en dur hors `socle.css`, et la copie du socle identique au gel. C'est la batterie centrale de cet ADR.
- **`pnpm verif:gel`** : l'empreinte SHA-256 de `mockups/socle.css` déclarée dans `mockups/GEL.md` correspond au fichier.
- **Batterie 11** — `pnpm verif:maquette V-41` : la bibliothèque de composants rendue par l'application est comparée à la planche gelée.
- Contrôle non encore outillé : la **fermeture** de l'inventaire de composants demande un lint d'import restreint, à écrire au lot T-009.
