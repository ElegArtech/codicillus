# Tâche T-035 — La recherche, l'espace public, et l'adresse non résolue

**Copie : `/tmp/wt-public`.** Port 5915. Vague 2. Dépendances **`T-030` et la vague 1 : livrées**.

**Lot de CÂBLAGE.** Tu ne crées pas d'écran : tu branches des écrans déjà conformes au pixel sur la
base, par un chargeur de route. Rapport **court**.

---

## 1 · Ce que tu branches

| Route | Écran | Régime |
|---|---|---|
| `/recherche` | **V-02** anonyme · **V-08** connecté | `publique` |
| `/guides/{identifiant}` | **V-03** Lecture publique | `resolution` |
| *(toute adresse non résolue)* | **V-04** anonyme · **V-26** connecté | `resolution` |

`src/lib/public/recherche.ts` et `src/lib/public/adresse-non-resolue.ts` existent déjà — **emploie-les,
ne les réécris pas.**

## 2 · La ligne de base, à relever avant d'écrire

```
find src/routes -name '+page.server.ts'        # les chargeurs déjà posés par la vague 1
pnpm test:etancheite                           # empreinte de départ, à faire DESCENDRE
```

**Cite les deux dans ton rapport.**

## 3 · Comment on branche, et pourquoi c'est sûr

`T-030` a livré `src/lib/donnees/` : des fonctions rendant **exactement les formes de
`seeds/corpus.ts`** depuis la base, et une batterie d'équivalence qui le prouve. **Lis son module, et
un chargeur de la vague 1 comme exemple, avant d'écrire.**

Les vues attendent déjà leurs données en propriété. Ton chargeur passe la même forme depuis la base :
les pixels ne bougent pas **par construction**. **Le banc n'attaque jamais les routes.**

### Les droits, appelés et jamais recopiés

`src/lib/droits/resolution.ts` est l'implémentation unique (`T-011`). L'identité est dans
`event.locals.identite` (`src/hooks.server.ts`). **Tu n'écris aucune seconde règle de droit.**

### Le point dur, et c'est la maquette qui le nomme

`mockups/V-04-non-trouvee-public.html:2219` appelle sa propre vérification *« la plus importante de
cette vue »* : **une adresse inexistante et une note existante non publique doivent produire un rendu
strictement identique.** C'est `RG-ACC-04` rendue mesurable, et la batterie 6 le compte aujourd'hui
comme **vacant** — parce que `/guides/{id}` n'est pas montée.

**Ce lot le rend mesurable.** Les deux cas passent par le même chemin de code (`ADR-007`) : il n'existe
pas de branche « interdit ». Et `docs/routes.md:163` autorise **une seule** différence : la chaîne
demandée, affichée telle quelle.

### La recherche, et ce qu'elle ne sera pas

`STACK` §4.2 veut Meilisearch pour les trois modes. **L'index n'est pas alimenté** — c'est `T-027`.
Tu interroges **PostgreSQL**, et tu livres le mode « mots-clés » seulement. Le mode « Sens » a besoin
de vecteurs : il n'existe pas, et il se **déclare indisponible**, jamais simulé (`P-10` — dégradation,
jamais panne ; `P-02` — aucune valeur illustrative).

**En anonyme**, `docs/routes.md:248` est formel : seuls `q`, `domaine` et `type` sont honorés. Un
paramètre `statut=` ou `visibilite=` présenté par un anonyme est **ignoré, jamais refusé** — un refus
révélerait l'existence du filtre.

### Et la demi-règle du chemin public, si `T-031` ne l'a pas fermée

`noteVisibleEnAnonyme` de `resolution.ts` exige `publique ET publiee`. Vérifie que ton chemin l'appelle,
et non `notesPubliques` de `seeds/corpus.ts`, qui n'en porte que la moitié (`ECART-047`).

## 4 · Ce que tu ne fais pas

- **Tu ne touches AUCUN fichier de `src/vues/`.** C'est la règle qui garantit le parallélisme et la
  conformité au gel. Si un écran te paraît devoir changer, **arrête-toi et déclare-le**.
- **Tu ne touches pas `seeds/corpus.ts`**, ni `src/lib/donnees/` de `T-030` : tu écris **ton** module,
  `src/lib/donnees/public.ts`.
- **Tu ne touches pas `package.json`**, ni `verif/**`, ni `docs/**`, ni `base/**`.
- **Tu n'installes aucune dépendance** (`P-24`).
- **Ne comble pas.** Ce que la base ne porte pas, tu le **déclares et le comptes**. En particulier : le mode « Sens », qui se déclare indisponible.
- **`cadrage/`, `mockups/`, `règles/`, `verif/references/` : lecture seule, mécaniquement.**

### Les fichiers que les autres lots occupent

**Vague 2, en parallèle :** `T-036` tient `src/routes/console/**`, `src/routes/bibliotheque/` et `donnees/consoles.ts` · `T-037` tient `src/routes/cartographie/**`, `carte-mentale/` et `donnees/outils.ts` · `T-038` tient `src/routes/mon-profil/`, `mot-de-passe-oublie/**` et `donnees/profil.ts`. **N'y touche pas.**

## 5 · Ne me crois pas sur parole

Onze de mes références de ligne étaient fausses au premier jet des contrats du 19 août. Vérifie celles
de ce contrat.

**Les bornes qui doivent t'arrêter :**

1. **Si brancher un écran fait bouger un seul des 409 couples**, arrête-toi.
2. **Si la base ne porte pas une donnée que l'écran affiche**, ne l'invente pas : nomme-la, compte-la.
   C'est `P-02` — aucune valeur illustrative, jamais.
3. **Si une règle de droit te paraît manquer**, appelle `resolution.ts` et déclare le manque.
4. **Si les deux cas de V-04 ne rendent pas un octet identique**, à la chaîne demandée près,
   arrête-toi : c'est la vérification que la maquette appelle la plus importante.

## 6 · Pièges qui te visent

**`P-2`** (« no tests » = échec) · **`P-5`** (une règle qu'aucun cas n'exerce) · **`P-9`, `P-20`,
`P-23`, `P-27`** — **décrire une forme, ne jamais la citer** : n'écris pas l'adresse du mode de
conception dans un commentaire · **`P-1`** (marqueur écrit, jamais `pgrep`) · **`P-22`** (nomme ce que
tu laisses debout).

## 7 · Critères de sortie

**Le but**
```
V-02, V-03, V-04, V-26 branchés → ta commande, sa sortie, écran par écran
le point dur de V-04            → adresse inexistante et note interne : rendu IDENTIQUE, mesuré
la recherche en base            → mode « mots-clés » ; le mode « Sens » DÉCLARÉ indisponible
l'anonyme                       → `statut=` et `visibilite=` IGNORÉS, jamais refusés
pnpm test:etancheite        → l'empreinte DESCEND. Dis de combien, et ce qui reste vacant
pnpm test:unit              → 0, tes cas inclus
```

**La condition — tout le reste inchangé**
```
pnpm verif:maquette:app     → 409 conformes · 0 écart        (UNE SEULE FOIS, à la fin)
pnpm check · verif:gel · verif:jetons · verif:inventaire · verif:demo:hors-production → 0
pnpm verif:donnees · test:aller-retour · verif:convertisseur · verif:fraicheur → 0
pnpm verif:base             → 18/18
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression · verif:vocabulaire → inchangés
pnpm verif:menus            → jamais plus haut qu'à ton arrivée, code 1 jamais 2
```

## 8 · Livrable

Le code, ses unitaires, et un rapport **court** : la ligne de base ; la table route → écran → requête ;
**les données que la base ne porte pas**, comptées ; le nouveau chiffre de `test:etancheite` ; la sortie
réelle des commandes du §7 ; tes écarts numérotés.

**Ne commite pas. Ne crée rien dans `docs/ecarts/` — je numérote.**
