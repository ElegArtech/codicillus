# Tâche T-046 — La batterie 6 rendue juste — l'artefact que trois lots ont signalé

**Copie : `/tmp/wt-banc-6`.** Port 5915. **Vague 4 — la dernière du plan.**

L'état à ton arrivée, mesuré : **32 routes montées sur 39**, les 41 vues acceptent leurs données,
**1422 tests**, banc **409 couples / 409 conformes / 0 écart**, `pnpm check` 0, `verif:menus` **VERT**,
`test:etancheite` rouge à `275/39/30/34`.

**Les dépendances de la pile sont DÉJÀ INSTALLÉES**, aux versions épinglées par
`cadrage/STACK-TECHNIQUE.md` §3. **Tu n'en installes aucune** (`P-24`) : si l'une te manque,
**arrête-toi et déclare-la**.

---

## 1 · Ce que tu livres

**Trois lots indépendants ont relevé le même défaut d'instrument**, et il fabrique la majorité des
défauts restants.

`verif/etancheite-attendu.mjs` lit la colonne « avecDroit » de `docs/routes.md` §5.5 **sans regarder la
variante d'adresse** : pour un persona habilité, la variante `inexistante` hérite donc de « servi ».
Conséquence — sur `…/ceci-n-existe-pas-dans-le-corpus/…`, la batterie **attend qu'une adresse
inexistante soit SERVIE**, et marque en plus le couple `asymetrique` parce que le côté existant sert
légitimement.

**Aucune implémentation correcte ne peut le satisfaire.** T-032 l'a chiffré à 16 + 16, T-034 à 10 + 10,
T-039 à 8. Et la batterie sait **déjà** traiter ce cas — `verif/etancheite.mjs:847`,
`raisonSansValeur !== null && forme === 'servi'` → `non-couverte` — mais ne l'applique pas à cette
variante.

**Tu le répares, et tu prouves que la réparation ne rend pas la batterie aveugle.**
`/guides/{identifiant}` échappe déjà au piège par un traitement par variante (`:409-419`) : c'est ton
modèle.

**Puis tu poses le seuil de la dette restante**, s'il en reste une — et **seulement** si elle ne se
ferme par aucun lot. Les vacuités ne sont **pas** une dette à seuiller : elles se referment route par
route, et `docs/orchestration.md` §4 interdit de seuiller ce qu'un lot referme.

## 2 · Ce qui te gouverne

**Les six règles de méthode d'un lot d'instrument**, `docs/orchestration.md` §1.2, s'appliquent
intégralement — et la deuxième est la tienne : **éprouve ta clé de rapprochement dans les deux sens.**
Un cas qui **doit** se rapprocher, un cas qui **ne doit pas**. `ECART-041` : une clé mal choisie a
fabriqué **31 faux défauts sur 31**.

**Et la quatrième** : prouve que la batterie sait dire non, sur deux genres de mutation, avec un
**refus de conclure** si la mutation est inerte. Les sondes existent — ne les affaiblis pas, ajoute.

**`ARB-052` et `ARB-057` gouvernent l'attendu** : une adresse dont la réponse dépend du corpus est
indiscernable ; une adresse dont la réponse ne dépend que de la présence d'une session redirige. Lis-les.

## 3 · Ce que tu ne fais pas

- **Tu n'installes aucune dépendance.** Elles sont posées ; `package.json` et `pnpm-lock.yaml` sont
  **interdits**.
- **Tu ne touches pas `verif/references/`** — écriture humaine seule, mécaniquement. Si le dépôt ne
  peut pas passer au vert, **livre rouge en l'expliquant** : cinq lots l'ont fait avant toi et ils
  avaient raison.
- **Ne joue ni `pnpm verif:base` ni `base:reversibilite`** — elles vident une base **partagée** par
  toutes les copies. `test:etancheite` est **à toi** : c'est ta batterie, et tu es seul à la jouer cette vague.
- **Ne comble pas.** Ce que la source ne dit pas, tu le **déclares et le comptes** (`règles/workflow_agentic.md` §4.15).
- **`cadrage/`, `mockups/`, `règles/` : lecture seule, mécaniquement.**
- **Ne commite pas. Ne crée rien dans `docs/ecarts/` — je numérote.**

### Les fichiers que les autres lots occupent

**Vague 4, en parallèle :** `T-047` tient `verif/banc/conditions.mjs` · `T-048` tient
`verif/tracabilite.mjs` et `docs/` · `T-049` tient `base/**`, `src/lib/base/**` et
`src/lib/donnees/lecture.ts`. **N'y touche pas.**

## 4 · Ne me crois pas sur parole

**Onze de mes références de ligne étaient fausses au premier jet des contrats de cette vague**, et
j'ai soutenu trois positions successives et fausses sur un même sujet avant qu'un lot ne me réfute
par la mesure. Vérifie ce contrat.

**Les bornes qui doivent t'arrêter :**

1. **Si un seul des 409 couples du banc bouge**, arrête-toi. Le gel est la loi, et il n'a pas bougé
   d'un pixel en trois jours de câblage.
2. **Si une source manque**, ne l'invente pas : nomme-la, compte-la, dis quel écran en dépend. C'est
   `P-02` — aucune valeur illustrative, jamais.
3. **Si la réparation fait descendre le compte de défauts sans qu'aucun défaut réel disparaisse**,
   c'est le bon résultat — mais **prouve-le ligne par ligne**, en montrant que chaque défaut retiré
   était l'artefact et non un manque.
4. **Si tu ne peux pas prouver que la batterie sait encore dire non après ta réparation**, ne la livre
   pas : `RA-01`, une batterie toujours verte ne prouve rien.

## 5 · Pièges qui te visent

**`P-2`** — « no tests » est un échec, pas « rien à faire ». · **`P-5`** — une règle qu'aucun cas
n'exerce est une règle qu'on espère. · **`P-26`** — un contrôle dont le seul cas d'épreuve est le
défaut qu'il trouve devient inerte en réussissant : donne-lui un cas **synthétique**. ·
**`P-9`, `P-20`, `P-23`, `P-27`** — **décrire une forme, ne jamais la citer** : ni chemin de `verif/`,
ni adresse de la route de conception dans un commentaire. · **`P-1`** — attends un marqueur écrit,
jamais `pgrep`. · **`P-14` et `P-29`** — sous huit copies concurrentes, l'horloge du banc et le cache
de Vite lâchent : si une batterie crashe sans rapport avec ton code, **rejoue**, et dis-le. ·
**`P-22`** — nomme dans ton rapport tout serveur ou conteneur que tu laisses debout.


## 6 · Critères de sortie

**Le but**
```
pnpm test:etancheite        → l'artefact retiré, et CHAQUE défaut retiré justifié un par un
les sondes                  → mordent toujours, refus de conclure sur l'inerte
le déterminisme             → trois exécutions, empreintes identiques
le seuil                    → posé SI et SEULEMENT SI la dette ne se ferme par aucun lot
pnpm test:unit              → 0, tes cas inclus, chacun nommé
```

**La condition — tout le reste, INCHANGÉ**
```
pnpm verif:maquette:app     → 409 conformes · 0 écart      (UNE SEULE FOIS, à la fin)
pnpm check                  → 0
pnpm verif:gel              → 43 empreintes intactes
pnpm verif:jetons · verif:inventaire · verif:demo:hors-production → 0
pnpm test:aller-retour · verif:convertisseur · verif:fraicheur → 0
pnpm verif:menus            → VERT (seuil de gel 3589). S'il rougit, tu as ajouté une entrée morte
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression · verif:vocabulaire → inchangés
pnpm test:etancheite        → son empreinte ne MONTE pas
```

## 7 · Livrable

Rapport **court** : la ligne de base ; ce que tu livres ; **ce que la source ne dit pas**, compté ; la
sortie réelle des commandes du §6 ; tes écarts numérotés `É-1`, `É-2`…
