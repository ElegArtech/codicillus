# Tâche T-042 — Les vues rendues capables — listes, dossier, lecture, éditeurs

**Copie : `/tmp/wt-props-b`.** Port 5916. **Vague 1b — le vrai gros du câblage.**

## 1 · Le mur, mesuré par trois lots indépendamment

**35 vues sur 41 importent les constantes du jeu de semence AU NIVEAU DU MODULE.** Un chargeur de route
leur passe `notes`, et **rien d'autre n'atteint l'écran**. Mesuré sur le produit :

- « En attente de révision = **3** » pour un compte qui lit **0** note, et pour l'administratrice qui en
  lit 26. Valeur figée, indépendante de la base et de l'identité — **`P-02` en défaut**.
- « **Bonjour Karim.** » et la pastille « Karim Belhadj » servies à **Sophie Nguyen**.
- `/notes/T-042` sert l'article de `n-restaurer-pg` **pour les 32 notes**.

Tes vues : **V-12 V-13 V-14 V-17 V-18**

## 2 · Ce que tu fais, et c'est mécanique

Pour chacune de tes vues, **remplace les imports de constantes par des propriétés OPTIONNELLES**.

```
AVANT :  import { UNIVERS, DOMAINES, MOI, INSTANCE } from '../../seeds/corpus';
APRÈS :  interface Proprietes {
             vecteur: …; notes: readonly Note[];
             univers?: readonly Univers[];      // défaut : UNIVERS
             domaines?: readonly Domaine[];     // défaut : DOMAINES
             compte?: UtilisateurCourant;       // défaut : MOI
             instance?: EtatDInstance;          // défaut : INSTANCE
         }
```

**Les quatre noms ci-dessus sont contractuels** — huit lots les emploieront. Les types sont **ceux de
`seeds/corpus.ts`**, tu n'en déclares aucun nouveau.

**Le défaut est la constante du jeu.** C'est ce qui garantit que **le banc ne bouge pas** : le mode de
conception ne passe rien de nouveau, donc les vues reçoivent exactement ce qu'elles avaient.

**V-14 est le cas dur, et le plus important du lot.** Son article vient de
`$lib/lecture/NoteDeDemonstration.svelte` puis `CorpsReference.svelte` (375 l.) et
`CorpsOperationnel.svelte` (103 l.) : **balisage figé transcrit des maquettes**. Résultat mesuré : la
route d'une note sert l'article de `n-restaurer-pg` **pour les 32 notes**.

**Il faut une propriété optionnelle portant la note à afficher et son corps rendu.** Le corps canonique
se rend par `rendreDocument` de `src/lib/contenu/rendu.ts` — `ADR-004` interdit tout second rendu, et
`pnpm verif:convertisseur` compte les implémentations.

**Le défaut reste la transcription figée**, sinon le banc bouge. Propriété absente, le balisage actuel à
l'identique ; propriété fournie, le document rendu. **Les 44 couples de V-14 sont le juge.**

**V-13 porte un domaine en dur** : `src/vues/V-13.svelte:76`, `const DOMAINE = 'Infrastructure'`.
`T-032` a mesuré qu'une adresse d'un autre domaine rend l'arborescence d'Infrastructure.

**V-17 et V-18 sont les éditeurs** : tu les rends capables, tu n'écris aucune saisie.

## 3 · La règle absolue

**`pnpm verif:maquette:app` doit rendre 409 conformes, 0 écart.** Tu touches `src/vues/`, donc **c'est
toi qui peux casser le gel**, et personne d'autre ne le rattrapera.

**Joue le banc sur TES vues d'abord** — `node verif/maquette.mjs V-12 V-13 V-14 V-17 V-18 --contre=app` —, corrige, et ne
lance la campagne complète qu'à la fin. Si un couple bouge, **arrête-toi et déclare-le** : la
conformité au gel prime sur le câblage.

**Deux pièges te visent, et ils mordent exactement sur ce que tu fais :**

- **`P-6`** — `prettier --write` réintroduit des blancs entre nœuds, et le relevé d'ordre de tabulation
  les voit : **27 couples en échec** pour cette seule cause. Le formatage fait partie de `pnpm check`,
  donc de ton critère de sortie : **un lot peut échouer parce qu'il a obéi à une autre obligation.**
  Parade : le commentaire d'exclusion de prettier, **forme exacte obligatoire**.
- **`P-8`** — Svelte élague les blancs en bord d'élément. `{n.univers} › ` perd son espace final :
  **porte l'espace dans l'expression**. Deux lots l'ont payé.

Et **`P-23`** : ne cite jamais un chemin de `verif/` dans un commentaire — décris-le. Je viens de nettoyer
15 fichiers pour cette raison.

## 4 · Ce que tu ne fais pas

- **Tu ne touches AUCUNE vue hors de ta liste.** Sept lots travaillent en parallèle sur les autres.
- **Tu ne branches aucune route** : les chargeurs existent ou viendront. Tu rends les vues *capables* de
  recevoir.
- **Tu ne changes AUCUN nœud, AUCUNE classe, AUCUN attribut de balisage.** Seule la **source** des
  données change. Si un rendu correct exige de changer le balisage, **arrête-toi et déclare-le**.
- **Tu ne touches pas `seeds/corpus.ts`**, ni `verif/**`, ni `docs/**`, ni `base/**`, ni `package.json`.
- **Tu n'installes aucune dépendance** (`P-24`).
- **Ne joue ni `pnpm verif:base` ni `base:reversibilite`** : base partagée.
- **`cadrage/`, `mockups/`, `règles/`, `verif/references/` : lecture seule, mécaniquement.**

## 5 · Critères de sortie

```
tes vues acceptent les 4 propriétés   → un unitaire par vue : la propriété fournie l'emporte, absente le défaut s'applique
node verif/maquette.mjs V-12 V-13 V-14 V-17 V-18 --contre=app  → conformes, 0 écart
pnpm verif:maquette:app               → 409 conformes · 0 écart      (UNE FOIS, à la fin)
pnpm check · verif:gel · verif:jetons · verif:inventaire · verif:demo:hors-production → 0
pnpm test:unit                        → 0, tes cas inclus
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression · verif:vocabulaire → inchangés
pnpm verif:menus                      → 81, code 1, jamais plus haut
```

## 6 · Livrable

Rapport **court** : la liste de tes vues et, pour chacune, les propriétés ajoutées ; les constantes qui
restaient et que tu n'as pas pu convertir, avec le motif ; la sortie du banc sur tes vues **et** de la
campagne ; tes écarts numérotés.

**Ne commite pas. Ne crée rien dans `docs/ecarts/`.**
