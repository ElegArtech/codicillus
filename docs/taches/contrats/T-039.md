# Tâche T-039 — L'historique et la comparaison de deux versions

**Copie : `/tmp/wt-histoire`.** Port 5919. Vague 3. Dépendances **`T-030` et la vague 1 : livrées**.

**Lot de CÂBLAGE.** Tu ne crées pas d'écran : tu branches des écrans déjà conformes au pixel sur la
base, par un chargeur de route. Rapport **court**.

---

## 1 · Ce que tu branches

| Route | Écran |
|---|---|
| `/notes/{identifiant}` *(panneau)* | **V-15** Historique |
| `/notes/{identifiant}/comparaison?versions={a}-{b}` | **V-16** Comparaison |

`T-030` a posé les tables de l'historique. **C'était le manque que le commanditaire a relevé : il
aurait dû être au schéma initial.**

## 2 · La ligne de base, à relever avant d'écrire

```
find src/routes -name '+page.server.ts'        # les chargeurs déjà posés par la vague 1
pnpm test:etancheite                           # empreinte de départ, à faire DESCENDRE
```

**Cite les deux dans ton rapport.**

## 3 · Comment on branche, et pourquoi c'est sûr

`T-030` a livré `src/lib/donnees/` : des fonctions rendant **exactement les formes de
`seeds/corpus.ts`** depuis la base, et une batterie d'équivalence qui le prouve. **Lis son module, et
un chargeur de la vague 1 comme exemple, avant d'écrire.**

Les vues attendent déjà leurs données en propriété. Ton chargeur passe la même forme depuis la base :
les pixels ne bougent pas **par construction**. **Le banc n'attaque jamais les routes.**

### Les droits, appelés et jamais recopiés

`src/lib/droits/resolution.ts` est l'implémentation unique (`T-011`). L'identité est dans
`event.locals.identite` (`src/hooks.server.ts`). **Tu n'écris aucune seconde règle de droit.**

### Les deux modes de comparaison, et le second est le point dur

`STACK` §4.5 :

- **Mode Texte** — différences ligne à ligne sur le **rendu Markdown** des deux versions, avec le
  paquet `diff`. **Vérifie qu'il est installé** ; s'il ne l'est pas, ne l'installe pas (`P-24`) :
  déclare-le, et emploie l'algorithme que le gel porte déjà.
- **Mode Visuel** — plus longue sous-séquence commune sur les **nœuds de premier niveau**, empreinte de
  nœud calculée sur son contenu normalisé. C'est `C-05`, et `ADR-003` dit que le format canonique est un
  arbre de nœuds **pour rendre cela possible**.

### `ARB-055` te gouverne, et c'est le piège de ce lot

Le gel porte `window.blocEnLignes` (`mockups/V-16-comparaison.html:1862-1878`), une linéarisation
« façon texte source ». `ARB-055` a tranché : **c'est un QUATRIÈME RENDU dérivé du document canonique,
pas un second convertisseur.**

Sa forme fait loi **à l'affichage** — `src/vues/V-16.svelte:149-173` la transcrit déjà, et le banc est
vert dessus. **Mais tu ne l'emploies JAMAIS pour reconstruire un document** : le jour où une de ces
lignes serait relue, ce serait un second convertisseur et `ADR-004` s'appliquerait plein.

`T-015` a laissé un garde-fou : `pnpm verif:convertisseur` imprime la liste des **frontaliers**, et
`src/vues/V-16.svelte` y figure avec la mention qu'il *« basculera en constat le jour où V-16 comparera
de vrais documents »*. **C'est ce jour-là. Attends-toi à ce que la batterie parle, et déclare ce qu'elle
dit.**

### Ce que les tables portent, et ce qu'elles ne portent pas

`T-030` a posé les colonnes des deux corps en `jsonb`, et **la semence les laisse vides** :
`CONTENU_VERSIONS` de `seeds/corpus.ts:1503-1705` est dans une forme ancienne (`BlocDeContenu`) que
`T-014` **et** `T-015` ont tous deux refusé de transposer, faute de cinq informations que le format
canonique exige.

**Tu ne la transposes pas davantage.** Tu dis ce que tu peux faire avec des versions sans corps, et
ce que ça coûte à V-16.

### `RG-M07-05` — la restauration est réversible par construction

*« La restauration écrit d'abord l'état courant comme nouvelle version, puis applique l'ancien. »*
L'opération est donc elle-même réversible, sans traitement particulier. Si tu la livres, livre-la ainsi.

## 4 · Ce que tu ne fais pas

- **Tu ne touches AUCUN fichier de `src/vues/`.** C'est la règle qui garantit le parallélisme et la
  conformité au gel. Si un écran te paraît devoir changer, **arrête-toi et déclare-le**.
- **Tu ne touches pas `seeds/corpus.ts`**, ni `src/lib/donnees/` de `T-030` : tu écris **ton** module,
  `src/lib/donnees/histoire.ts`.
- **Tu ne touches pas `package.json`**, ni `verif/**`, ni `docs/**`, ni `base/**`.
- **Tu n'installes aucune dépendance** (`P-24`).
- **Ne comble pas.** Ce que la base ne porte pas, tu le **déclares et le comptes**. En particulier : les corps des anciennes versions, que la semence laisse vides.
- **`cadrage/`, `mockups/`, `règles/`, `verif/references/` : lecture seule, mécaniquement.**

### Les fichiers que les autres lots occupent

**Vague 3, en parallèle :** `T-040` tient `src/routes/importer/` et `src/lib/donnees/import.ts`. Et **`src/routes/notes/[identifiant]/+page.server.ts` appartient à `T-033`, livré** : tu ajoutes `comparaison/`, tu ne réécris pas le sien.

## 5 · Ne me crois pas sur parole

Onze de mes références de ligne étaient fausses au premier jet des contrats du 19 août. Vérifie celles
de ce contrat.

**Les bornes qui doivent t'arrêter :**

1. **Si brancher un écran fait bouger un seul des 409 couples**, arrête-toi.
2. **Si la base ne porte pas une donnée que l'écran affiche**, ne l'invente pas : nomme-la, compte-la.
   C'est `P-02` — aucune valeur illustrative, jamais.
3. **Si une règle de droit te paraît manquer**, appelle `resolution.ts` et déclare le manque.
4. **Si `verif:convertisseur` bascule `src/vues/V-16.svelte` en constat**, c'était annoncé : déclare
   ce qu'il dit, et ne desserre pas le prédicat. `T-015` É-4 explique pourquoi il est ce qu'il est.
5. **Si tu te trouves à relire une ligne de `blocEnLignes` pour reconstruire un document**, arrête-toi :
   c'est un second convertisseur, et `ADR-004` l'interdit nommément.

## 6 · Pièges qui te visent

**`P-2`** (« no tests » = échec) · **`P-5`** (une règle qu'aucun cas n'exerce) · **`P-9`, `P-20`,
`P-23`, `P-27`** — **décrire une forme, ne jamais la citer** : n'écris pas l'adresse du mode de
conception dans un commentaire · **`P-1`** (marqueur écrit, jamais `pgrep`) · **`P-22`** (nomme ce que
tu laisses debout).

## 7 · Critères de sortie

**Le but**
```
V-15 branchée                → l'historique réel d'une note, depuis la table des versions
V-16 mode Texte              → différences ligne à ligne sur le rendu, et tu dis avec quel algorithme
V-16 mode Visuel             → blocs identiques alignés (C-05), sur les nœuds de premier niveau
ce que les versions n'ont pas → les corps vides, et ce que ça coûte à V-16 — compté
pnpm test:etancheite        → l'empreinte DESCEND. Dis de combien, et ce qui reste vacant
pnpm test:unit              → 0, tes cas inclus
```

**La condition — tout le reste inchangé**
```
pnpm verif:maquette:app     → 409 conformes · 0 écart        (UNE SEULE FOIS, à la fin)
pnpm check · verif:gel · verif:jetons · verif:inventaire · verif:demo:hors-production → 0
pnpm verif:donnees · test:aller-retour · verif:convertisseur · verif:fraicheur → 0
pnpm verif:base             → 18/18
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression · verif:vocabulaire → inchangés
pnpm verif:menus            → jamais plus haut qu'à ton arrivée, code 1 jamais 2
```

## 8 · Livrable

Le code, ses unitaires, et un rapport **court** : la ligne de base ; la table route → écran → requête ;
**les données que la base ne porte pas**, comptées ; le nouveau chiffre de `test:etancheite` ; la sortie
réelle des commandes du §7 ; tes écarts numérotés.

**Ne commite pas. Ne crée rien dans `docs/ecarts/` — je numérote.**
