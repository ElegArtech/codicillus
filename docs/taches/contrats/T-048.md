# Tâche T-048 — Le contrôle de traçabilité — six numéros cités sans pièce

**Copie : `/tmp/wt-tracabilite`.** Port 5917. **Vague 4 — la dernière du plan.**

L'état à ton arrivée, mesuré : **32 routes montées sur 39**, les 41 vues acceptent leurs données,
**1422 tests**, banc **409 couples / 409 conformes / 0 écart**, `pnpm check` 0, `verif:menus` **VERT**,
`test:etancheite` rouge à `275/39/30/34`.

**Les dépendances de la pile sont DÉJÀ INSTALLÉES**, aux versions épinglées par
`cadrage/STACK-TECHNIQUE.md` §3. **Tu n'en installes aucune** (`P-24`) : si l'une te manque,
**arrête-toi et déclare-la**.

---

## 1 · Ce que tu livres

`ECART-043` l'a mesuré : le dépôt cite **six numéros dont la pièce n'existe pas**, depuis **dix**
endroits.

```
ECART-029  ← verif/banc/mode-demo.mjs, verif/references/protocole-app.json
ECART-030  ← docs/arbitrages.md (ARB-024 déclare y répondre)
ECART-034  ← docs/arbitrages.md (ARB-025 déclare y répondre)
ECART-042  ← verif/references/a11y-seuil.json, docs/reprise.md, journal, orchestration
ARB-045    ← verif/etats.mjs ×4 + ses unitaires ×2 — dont une chaîne IMPRIMÉE À L'EXÉCUTION
ARB-046    ← src/lib/coquille/Rail.svelte, qui cite son texte propre
```

**Deux sont dans `verif/references/`**, en écriture humaine seule — les artefacts les plus opposables
du dépôt. `a11y-seuil.json:16-21` justifie **deux lignes volontairement absentes d'un seuil** en
renvoyant à une pièce inexistante : le fichier existe pour qu'un tiers contrôle la décision, et il ne
peut pas.

**Et `ARB-045` est le plus grave** : c'est le numéro qui **autorise une correction d'instrument**, le
geste le plus surveillé de ce dépôt, et la batterie 9 l'imprime à chaque exécution.

**Tu livres un contrôle qui REFUSE toute référence sans pièce porteuse** — `ECART-0xx`, `ARB-xxx`,
`É-n`, `P-xx` — et qui lit `verif/`, `src/`, `base/`, `seeds/` **et** `docs/`, pas seulement `docs/`.

## 2 · Ce qui te gouverne

**Le contrôle doit rougir aujourd'hui, et c'est la preuve qu'il mord.** Posé sur un dépôt sans
pointeur mort, il serait vert par vacuité — `RA-01`, et c'est exactement le motif pour lequel le filtre
d'`ARB-013` est resté inerte huit lots.

**Deux réparations sont connues et tu les appliques :**

- **`ARB-045` ≡ `ARB-048`.** `ECART-043` en donne trois recoupements : même sujet, même lot `T-072`, et
  la « première exigence » est mot pour mot la même des deux côtés. **Vérifie-les, puis renumérote les
  huit citations.** Si l'hypothèse ne tient pas, dis-le et ne renumérote rien.
- **`ARB-046` a été révoqué puis EFFACÉ du registre**, alors qu'`ARB-047` s'y adosse — son titre même
  est *« ARB-046 est révoqué »*. **Un arbitrage révoqué se marque révoqué, il ne s'efface pas** : c'est
  le motif exact pour lequel `cadrage/` n'est jamais corrigé et l'errata existe. Réinscris-le en
  « révoqué », à partir de ce que `Rail.svelte` en cite — et **rien de plus**.

**Les quatre écarts manquants, tu ne les reconstitues PAS.** `git log -S` ne rend que les commits qui
les citent. Écrire un dossier depuis un résumé de deuxième main produirait une pièce d'apparence
opposable et de contenu deviné : `P-21` vaut aussi pour qui documente. **Ils restent la dette de
départ du contrôle**, bornée par un seuil qui ne peut que descendre.

## 3 · Ce que tu ne fais pas

- **Tu n'installes aucune dépendance.** Elles sont posées ; `package.json` et `pnpm-lock.yaml` sont
  **interdits**.
- **Tu ne touches pas `verif/references/`** — écriture humaine seule, mécaniquement. Si le dépôt ne
  peut pas passer au vert, **livre rouge en l'expliquant** : cinq lots l'ont fait avant toi et ils
  avaient raison.
- **Ne joue ni `pnpm verif:base` ni `base:reversibilite`** — elles vident une base **partagée** par
  toutes les copies. 
- **Ne comble pas.** Ce que la source ne dit pas, tu le **déclares et le comptes** (`règles/workflow_agentic.md` §4.15).
- **`cadrage/`, `mockups/`, `règles/` : lecture seule, mécaniquement.**
- **Ne commite pas. Ne crée rien dans `docs/ecarts/` — je numérote.**

### Les fichiers que les autres lots occupent

**Vague 4, en parallèle :** `T-046` tient `verif/etancheite*.mjs` · `T-047` tient
`verif/banc/conditions.mjs` et la section `P-14` de `CLAUDE.md` · `T-049` tient `base/**` et
`src/lib/base/**`. **`docs/` est à toi**, sauf la section `P-14` de `CLAUDE.md`.

## 4 · Ne me crois pas sur parole

**Onze de mes références de ligne étaient fausses au premier jet des contrats de cette vague**, et
j'ai soutenu trois positions successives et fausses sur un même sujet avant qu'un lot ne me réfute
par la mesure. Vérifie ce contrat.

**Les bornes qui doivent t'arrêter :**

1. **Si un seul des 409 couples du banc bouge**, arrête-toi. Le gel est la loi, et il n'a pas bougé
   d'un pixel en trois jours de câblage.
2. **Si une source manque**, ne l'invente pas : nomme-la, compte-la, dis quel écran en dépend. C'est
   `P-02` — aucune valeur illustrative, jamais.
3. **Si `ARB-045` n'est PAS `ARB-048`**, ne renumérote rien : dis ce que tu as trouvé.
4. **Si le contrôle ne peut pas distinguer une citation d'une définition** — une prose qui décrit un
   numéro sans y renvoyer —, dis-le et compte les faux positifs. Ne desserre pas le crible pour obtenir
   un chiffre plus flatteur.

## 5 · Pièges qui te visent

**`P-2`** — « no tests » est un échec, pas « rien à faire ». · **`P-5`** — une règle qu'aucun cas
n'exerce est une règle qu'on espère. · **`P-26`** — un contrôle dont le seul cas d'épreuve est le
défaut qu'il trouve devient inerte en réussissant : donne-lui un cas **synthétique**. ·
**`P-9`, `P-20`, `P-23`, `P-27`** — **décrire une forme, ne jamais la citer** : ni chemin de `verif/`,
ni adresse de la route de conception dans un commentaire. · **`P-1`** — attends un marqueur écrit,
jamais `pgrep`. · **`P-14` et `P-29`** — sous huit copies concurrentes, l'horloge du banc et le cache
de Vite lâchent : si une batterie crashe sans rapport avec ton code, **rejoue**, et dis-le. ·
**`P-22`** — nomme dans ton rapport tout serveur ou conteneur que tu laisses debout.
 · **`P-18`** — `grep -a` est nécessaire sur `src/lib/base/commandes.ts`, qui porte deux octets NUL : `grep` y répond silencieusement rien, exit 1, sans message.

## 6 · Critères de sortie

**Le but**
```
pnpm verif:tracabilite      → rouge sur la dette réelle, et le compte JUSTIFIÉ ligne par ligne
la sonde                    → deux mutations, code inversé, refus de conclure sur l'inerte
ARB-045                     → renumérotée sur les huit citations, OU l'hypothèse réfutée avec sa preuve
ARB-046                     → réinscrite au registre, marquée révoquée, sans un mot d'invention
le seuil                    → posé sur ce qui ne se reconstitue pas, et il ne peut que descendre
pnpm test:unit              → 0, tes cas inclus, chacun nommé
```

**La condition — tout le reste, INCHANGÉ**
```
pnpm verif:maquette:app     → 409 conformes · 0 écart      (UNE SEULE FOIS, à la fin)
pnpm check                  → 0
pnpm verif:gel              → 43 empreintes intactes
pnpm verif:jetons · verif:inventaire · verif:demo:hors-production → 0
pnpm test:aller-retour · verif:convertisseur · verif:fraicheur → 0
pnpm verif:menus            → VERT (seuil de gel 3589). S'il rougit, tu as ajouté une entrée morte
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression · verif:vocabulaire → inchangés
pnpm test:etancheite        → son empreinte ne MONTE pas
```

## 7 · Livrable

Rapport **court** : la ligne de base ; ce que tu livres ; **ce que la source ne dit pas**, compté ; la
sortie réelle des commandes du §6 ; tes écarts numérotés `É-1`, `É-2`…
