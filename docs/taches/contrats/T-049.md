# Tâche T-049 — Les trois lacunes fermables, et les pièces jointes

**Copie : `/tmp/wt-lacunes2`.** Port 5918. **Vague 4 — la dernière du plan.**

L'état à ton arrivée, mesuré : **32 routes montées sur 39**, les 41 vues acceptent leurs données,
**1422 tests**, banc **409 couples / 409 conformes / 0 écart**, `pnpm check` 0, `verif:menus` **VERT**,
`test:etancheite` rouge à `275/39/30/34`.

**Les dépendances de la pile sont DÉJÀ INSTALLÉES**, aux versions épinglées par
`cadrage/STACK-TECHNIQUE.md` §3. **Tu n'en installes aucune** (`P-24`) : si l'une te manque,
**arrête-toi et déclare-la**.

---

## 1 · Ce que tu livres

`pnpm verif:donnees` rend **13 formes, 0 divergence, 6 lacunes**. « 0 divergence » veut dire que
partout où la base porte la donnée, la couche la rend à l'identique du jeu. **Trois lacunes sur six se
ferment ; trois ne se ferment pas.**

**Celles qui se ferment :**

| # | Lacune | Ce qui la referme |
|---|---|---|
| `Note.pj` | 7 notes, 13 pièces déclarées, **0 ligne en base** — `semence.ts` n'a pas de fabrique de pièce jointe | une semence des pièces jointes. Le jeu porte les **décomptes**, jamais les noms ; deux pièces réelles sont au balisage de `V-14:1831-1841` |
| `Template.utilisations` | **arithmétiquement impossible** — voir ci-dessous | rien : c'est un défaut du gel |
| `Compte.derniere` | la colonne porte l'instant depuis la migration 005 ; **le libellé relatif n'a pas de fabrique** | une fabrique de libellé — mais aucune source ne donne le seuil où « N jours » devient « N mois » |

**`Template.utilisations` est un DÉFAUT DU GEL, et il faut le porter au dossier de regel.**
`mockups/V-31-console-templates.html:1655` annonce « les **72** notes déjà créées à partir de ces
templates » (34+12+7+19) et `:3348` les rend note par note. **Le corpus compte 32 notes.** Aucune
colonne de provenance ne peut porter 72 provenances sur 32 lignes. `T-030b` a refusé de poser la
colonne — rien ne l'écrirait, rien ne la lirait, `P-5`. **Tu ne la poses pas davantage : tu écris
l'entrée du dossier de regel**, la première depuis qu'il a été vidé.

**Et `verif:donnees` ne peut PAS atteindre zéro, pour une raison d'instrument que `T-030b` a
établie** : `lacunes()` de `src/lib/donnees/equivalence.ts:244` est un **tableau de six littéraux**, et
la fonction `chiffrerLesLacunes()` que son commentaire promet **n'existe nulle part**. `total =
divergences + lesLacunes.length ≥ 6`, quoi qu'une migration fasse. **Répare-le : une lacune refermée
doit disparaître du rapport d'elle-même.** Et le geste est **atomique** — instrument et couche en une
fois —, sinon rendre ce que la base porte désormais ferait rougir la batterie **pour avoir eu
raison**.

## 2 · Ce qui te gouverne

**`P-02` te vise sur les pièces jointes.** Le jeu porte des décomptes, pas des noms de fichiers : en
fabriquer 33 valeurs pour satisfaire trois colonnes `NOT NULL` serait la valeur illustrative
interdite. **Deux pièces réelles existent au gel** (`V-14:1831-1841`) ; pour les onze autres, tranche
et **dis ce que tu as tranché** — ou livre la lacune ouverte en l'expliquant. `T-030b` a choisi de
l'ouvrir ; tu peux le contredire, avec une mesure.

**Et `equivalence.ts` est l'instrument qui te juge.** Le réparer est légitime — son commentaire promet
un comportement qu'il n'a pas —, mais **prouve que le réparé sait encore dire non** : une lacune
inventée doit le faire rougir, et sa sonde doit refuser de conclure sur une mutation inerte.

## 3 · Ce que tu ne fais pas

- **Tu n'installes aucune dépendance.** Elles sont posées ; `package.json` et `pnpm-lock.yaml` sont
  **interdits**.
- **Tu ne touches pas `verif/references/`** — écriture humaine seule, mécaniquement. Si le dépôt ne
  peut pas passer au vert, **livre rouge en l'expliquant** : cinq lots l'ont fait avant toi et ils
  avaient raison.
- **Ne joue ni `pnpm verif:base` ni `base:reversibilite`** — elles vident une base **partagée** par
  toutes les copies. **`base/**` et `src/lib/base/**` sont à toi seul**, et toi seul joues `verif:base` et `base:reversibilite` cette vague — joue-les tôt, et ressème après (`base:semer`).
- **Ne comble pas.** Ce que la source ne dit pas, tu le **déclares et le comptes** (`règles/workflow_agentic.md` §4.15).
- **`cadrage/`, `mockups/`, `règles/` : lecture seule, mécaniquement.**
- **Ne commite pas. Ne crée rien dans `docs/ecarts/` — je numérote.**

### Les fichiers que les autres lots occupent

**Vague 4, en parallèle :** `T-046` tient `verif/etancheite*.mjs` · `T-047` tient
`verif/banc/conditions.mjs` · `T-048` tient `verif/tracabilite.mjs` et `docs/` — **sauf
`docs/dossier-regel.md`, qui est à toi** pour la seule entrée `Template.utilisations`.

## 4 · Ne me crois pas sur parole

**Onze de mes références de ligne étaient fausses au premier jet des contrats de cette vague**, et
j'ai soutenu trois positions successives et fausses sur un même sujet avant qu'un lot ne me réfute
par la mesure. Vérifie ce contrat.

**Les bornes qui doivent t'arrêter :**

1. **Si un seul des 409 couples du banc bouge**, arrête-toi. Le gel est la loi, et il n'a pas bougé
   d'un pixel en trois jours de câblage.
2. **Si une source manque**, ne l'invente pas : nomme-la, compte-la, dis quel écran en dépend. C'est
   `P-02` — aucune valeur illustrative, jamais.
3. **Si les onze pièces jointes ne peuvent pas être semées sans inventer**, livre la lacune ouverte.
   Un chiffre faux vaut moins qu'un manque déclaré.
4. **Si réparer `equivalence.ts` fait rougir la batterie sur des formes que la couche rend
   correctement**, tu as fait le geste à moitié : il est atomique, instrument **et** couche.

## 5 · Pièges qui te visent

**`P-2`** — « no tests » est un échec, pas « rien à faire ». · **`P-5`** — une règle qu'aucun cas
n'exerce est une règle qu'on espère. · **`P-26`** — un contrôle dont le seul cas d'épreuve est le
défaut qu'il trouve devient inerte en réussissant : donne-lui un cas **synthétique**. ·
**`P-9`, `P-20`, `P-23`, `P-27`** — **décrire une forme, ne jamais la citer** : ni chemin de `verif/`,
ni adresse de la route de conception dans un commentaire. · **`P-1`** — attends un marqueur écrit,
jamais `pgrep`. · **`P-14` et `P-29`** — sous huit copies concurrentes, l'horloge du banc et le cache
de Vite lâchent : si une batterie crashe sans rapport avec ton code, **rejoue**, et dis-le. ·
**`P-22`** — nomme dans ton rapport tout serveur ou conteneur que tu laisses debout.
 · **`P-17`** — accent grave dans un modèle littéral : tu écris du SQL.

## 6 · Critères de sortie

**Le but**
```
pnpm verif:donnees          → les lacunes refermées DISPARAISSENT du rapport d'elles-mêmes
Note.pj                     → semée, ou la lacune livrée ouverte avec son motif
Compte.derniere             → une fabrique de libellé, ou le manque de source déclaré
Template.utilisations       → l'entrée du dossier de regel écrite : 72 annoncées, 32 possibles
migration 006               → s'il en faut une : montée, descente, empreinte identique
pnpm verif:base             → 18/18, et la base laissée SEMÉE
pnpm test:unit              → 0, tes cas inclus, chacun nommé
```

**La condition — tout le reste, INCHANGÉ**
```
pnpm verif:maquette:app     → 409 conformes · 0 écart      (UNE SEULE FOIS, à la fin)
pnpm check                  → 0
pnpm verif:gel              → 43 empreintes intactes
pnpm verif:jetons · verif:inventaire · verif:demo:hors-production → 0
pnpm test:aller-retour · verif:convertisseur · verif:fraicheur → 0
pnpm verif:menus            → VERT (seuil de gel 3589). S'il rougit, tu as ajouté une entrée morte
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression · verif:vocabulaire → inchangés
pnpm test:etancheite        → son empreinte ne MONTE pas
```

## 7 · Livrable

Rapport **court** : la ligne de base ; ce que tu livres ; **ce que la source ne dit pas**, compté ; la
sortie réelle des commandes du §6 ; tes écarts numérotés `É-1`, `É-2`…
