# Tâche T-012 — Authentification, sessions, et le ralentissement des tentatives

**Copie de travail : `/tmp/wt-auth`.** Port : 5911. Vague 1, criticité **haute**.
Dépendance `T-011` : **livrée** (commit `96fd53b`).

Plan §6.3, ligne du lot, recopiée :

> `T-012 | Authentification et sessions | T-011 | vague 1 | haute | V-05 | M16.1, RG-M16-01,
> RG-ACC-02, RG-ACC-03` — **critères** : `Batteries 3, 6, 11 ; ralentissement mesuré`

---

## 1 · Ce que ce lot ferme, et d'où le fait sort

**Le produit n'a aucune authentification.** `src/vues/V-05.svelte` rend un état de maquette et le dit
en propres termes à son en-tête (`:9-16`) : *« Il rend un ÉTAT DE MAQUETTE. Il n'authentifie personne,
n'ouvre aucune session, ne compte aucune tentative et ne ralentit rien. »* Et
`src/routes/connexion/+page.svelte:11-14` : *« pas de chargeur, pas de garde de droit, pas
d'authentification […] l'étanchéité et la résolution des droits appartiennent à T-011 et T-012. »*

Trois faits vérifiés, fichier ouvert :

| Fait | Où |
|---|---|
| `src/lib/base/schema.ts` porte 18 tables et **aucune** ne stocke un mot de passe, une session ou une tentative | `comptes` à `:102-121` — `identifiant`, `nom`, `courriel`, `role`, `actif`, `motDePasseVerrouille`, `arriveLe` ; pas de condensat |
| `base/migrations/` s'arrête à **002** | `001_extensions`, `002_socle`, montée et descente |
| aucun `hooks.server.ts` n'existe | `src/` porte `app.d.ts`, `app.html`, `lib`, `routes`, `socle.css`, `vues` |

**Trois conséquences documentaires que ce lot lève** : `RG-M14-08` n'a « aucun point d'application »
(`T-011` É-6) ; `docs/routes.md:460` interdit à tout lot de déclarer `RG-ACC-04` tenue ; et
`T-011` a livré une résolution de droits que **rien n'appelle**, faute d'identité réelle.

**Relève la ligne de base toi-même avant d'écrire, et cite-la.**

---

## 2 · Ce qui est déjà tranché, et que tu ne rouvres pas

| Décision | Ce qu'elle t'impose |
|---|---|
| **`STACK-TECHNIQUE.md` §4.7** (`:317-328`) | mots de passe **`@node-rs/argon2` 2.1.0, Argon2id** ; sessions par **jetons opaques en base**, cookie `HttpOnly`, `SameSite=Lax`, `Secure` ; tentatives répétées **ralenties puis bloquées temporairement, compteur en base** ; droits résolus par requête récursive ; refus/inexistence par **le même chemin de code** |
| **`ADR-007`** | refus et inexistence passent par le même chemin ; il n'existe pas de branche « interdit » |
| **`ARB-005`** | les deux régimes de refus, et lequel porte sur quoi. **En cas de doute, l'indiscernable l'emporte** |
| **`ARB-038`** | la base ne se configure **jamais** par une URI |
| **`ARB-050`** | **et `src/lib/base/connexion.ts` ne l'applique pas.** C'est ton second livrable — §4 |
| **`ARB-011`** | l'état « chargement » rend la notification, pas la minuterie |
| **`P-09`, `ADR-011`** | une action interdite n'est pas rendue |

---

## 3 · Ce que le gel décide, et que tu n'as donc pas à décider

**Tout est écrit. Ouvre les fichiers.** Les libellés ci-dessous sont **contractuels au caractère
près** — le banc mesure V-05 au pixel, et 409 couples sur 409 en dépendent.

### 3.1 · Les champs du formulaire — `mockups/V-05-connexion.html:551-591`

`identifiant` (texte, `autocomplete="username"`), `motdepasse` (`autocomplete="current-password"`),
`souvenir` (case à cocher, « Se souvenir de moi · À éviter sur un poste partagé »).

### 3.2 · Les trois arrivées — `mockups/V-05-connexion.html:667-679`, table `ARRIVEES`

| Clé | Titre | Texte |
|---|---|---|
| `protegee` | « Vous devez être connecté pour accéder à cette page » | « Après connexion, vous serez ramené là où vous alliez. » |
| `expiree` | « Session expirée » | « Votre session a été fermée après une période d'inactivité. Reconnectez-vous pour reprendre. » |
| `directe` | **`null`** — le bandeau est masqué, sans que sa classe ni son contenu ne changent |

`src/vues/V-05.svelte:112-127` porte déjà cette table, **et son subtil détail** : `directe` masque
mais conserve le contenu du balisage. Ne le touche pas.

### 3.3 · L'échec — `mockups/V-05-connexion.html:697-708`, fonction `echec()`

Titre « **Identifiant ou mot de passe incorrect** », texte « **Vérifiez votre saisie, puis
réessayez.** », `data-etat="erreur"` sur **les deux** champs, mot de passe vidé, focus et sélection
sur l'identifiant.

Le commentaire du gel (`:691-696`) est la règle, et il est plus strict que le cahier :
*« message strictement générique. Ni l'existence de l'identifiant, ni la validité du mot de passe ne
sont révélées. Un seul et même message quel que soit ce qui a échoué, et **aucun marquage sur l'un
des deux champs plutôt que sur l'autre**. »*

> **Et cela vaut aussi du temps de réponse.** Un identifiant inconnu qui répond sans calculer de
> condensat répond plus vite qu'un mot de passe faux — c'est un canal, et `ARB-005` dit qu'en cas de
> doute l'indiscernable l'emporte. **Mesure-le, et dis ce que tu mesures.**

### 3.4 · Le blocage — `mockups/V-05-connexion.html:713-742`, fonction `verrouiller(secondes)`

`fieldset#champs` **désactivé**, titre « **Trop de tentatives** », texte « Nouvelle tentative possible
dans **{N min NN s}**. », décompte à la seconde, et au terme : réactivation, retour au bandeau
d'arrivée, focus sur l'identifiant. Appelé avec **90** secondes (`:777`).

> **Le décompte est un comportement temporisé, et `CLAUDE.md` §4 le range parmi les interdictions de
> conclure** : aucune batterie du dépôt ne le juge. Il relève de `T-017`. **Ce lot livre la décision
> serveur** — combien de tentatives, quel ralentissement, quelle durée de blocage, et le compteur en
> base —, **pas l'animation**. Ne touche pas `src/vues/V-05.svelte`.
>
> **Les 90 secondes du gel sont la valeur d'une démonstration, pas un barème.** `RG-M16-01`
> (`:1279`) exige « un message explicite indiquant la durée d'attente » — donc que la durée soit
> **transmise**, non qu'elle vaille 90. Si tu retiens un barème progressif, la valeur transmise est
> celle du barème. Dis-le.

### 3.5 · Les redirections — `docs/routes.md:320-329`, §5.2, table recopiée

| Situation | Comportement |
|---|---|
| Route protégée sans session | `302 → /connexion?motif=page-protegee&suite={chemin}` |
| Session expirée | `302 → /connexion?motif=session-expiree&suite={chemin}` puis restauration de `{suite}` |
| Après connexion | `{suite}` si présent, sinon `/` |
| Après déconnexion | `302 → /` (espace public), **jamais** une page d'erreur |

Et `:329` : *« `?suite=` n'accepte qu'un chemin absolu interne : une valeur externe est **ignorée et
remplacée par `/`** »*. Ce n'est pas un refus, c'est une substitution : ne rends pas 400.

`docs/routes.md:286` relie les valeurs de `?motif=` aux trois positions de la planche V-05 :
`page-protegee` / `session-expiree` / *(absent = accès direct)* → `protegee` / `expiree` / `directe`.
**La correspondance est déjà faite ; ne l'invente pas.**

`docs/routes.md:116` inventorie déjà `/deconnexion` comme route d'action, sans vue, niveau connecté.
**Elle est dans les 39 routes du §3** : l'implémenter ne fait donc **pas** bouger `ATTENDU_ROUTES`.
En revanche, **n'ajoute aucune ligne au §3 de `docs/routes.md`** — `verif/menus.mjs:923` porte
`ATTENDU_ROUTES = 39` en dur et sort en **code 2** à la première divergence.

**« Sinon `/` » n'est pas une contradiction avec V-05.** `docs/routes.md:98-99` : `/` rend V-01 sans
session et **V-07 accueil contributeur avec session**. La notification du gel
(`V-05:774`, « accueil contributeur, vue V-07 ») et « sinon `/` » désignent le même endroit.
N'invente pas `/accueil`.

### 3.6 · La durée de session — et c'est un délai d'**inactivité**

`mockups/V-33-console-configuration.html:1359-1361` :

> « Durée de session — **Délai d'inactivité** au bout duquel la session se ferme, **sauf si
> l'utilisateur a choisi de rester connecté**. »

Choix offerts : `[30, 60, 120, 240, 480]` minutes (`V-33:2963`). Défaut **120** (`V-33:2663`).
**Le paramètre est déjà semé** : `src/lib/base/semence.ts:484` écrit
`{ cle: 'duree_session', valeur: CONFIG.dureeSession }` dans la table `parametres`
(`schema.ts:345-349`).

**Donc, et sans que tu aies rien à décider** : la session ordinaire se ferme sur inactivité, la durée
est lue en base et non codée en dur (`RG-M14-08`), et « se souvenir de moi » **exempte du délai
d'inactivité**. C'est ce que « prolongeant la session » de `CDC:1275` veut dire, et le gel le dit
mieux que le cahier.

---

## 4 · Second livrable — `ARB-050`, et c'est un défaut réel

**Lis `ARB-050` en entier.** En deux lignes : `ARB-038` a décidé que la base ne se configure que par
variables séparées et que « `URL_BASE` disparaît du contrat de déploiement » ; `compose.yaml`
l'applique et passe à `app` les cinq `HOTE_BASE`, `PORT_BASE`, `UTILISATEUR_BASE`, `MDP_BASE`,
`NOM_BASE` ; et `src/lib/base/connexion.ts` **n'en lit aucune** (`:60-67`) tout en gardant vivant le
chemin `URL_BASE`, qui rend **une chaîne** (`:80-83`).

Deux conséquences : l'application en conteneur ne peut pas se connecter, et `P-13` est réouvert par la
porte de derrière.

**Ce que tu livres** : les cinq `*_BASE` lues, le chemin `URL_BASE` retiré, les `*_POSTGRES`
**conservés** (ARB-038 les garde pour le serveur PostgreSQL, et les commandes de base les emploient),
et **deux unitaires qui exercent le cas** — l'un échouant si `URL_BASE` redevient accepté, l'autre
prouvant que les cinq `*_BASE` produisent un **objet**, `connectionString` absent du résultat.

**Sans le second cas, la correction est espérée et non posée.** C'est `P-5`, et c'est précisément
pourquoi ce défaut a survécu à une batterie verte : rien n'exerce le chemin de l'application
conteneurisée.

---

## 5 · Ce que tu ne fais pas

- **Tu n'écris pas la batterie 6.** Elle est le critère du plan pour ce lot, et **je l'en retire, en
  arbitrage délégué.** Motif : une batterie est un **instrument**, et `docs/orchestration.md` §1.2 lui
  impose six règles de méthode propres — auditer les deux côtés, éprouver la clé de rapprochement dans
  les deux sens, éprouver la table, prouver qu'elle sait dire non, prouver le déterminisme sur trois
  exécutions, et ne pas se donner son seuil. **Un instrument écrit par le lot qu'il juge est un
  instrument complaisant** ; et `T-011` a déjà refusé de la livrer pour une raison qui tient toujours.
  Elle part en lot propre dès que ton lot rend.
  **Mais tu lui prépares le terrain, et c'est un livrable** : décris, en fin de rapport, **ce que ton
  code rend mesurable** — les personas qu'on peut désormais forger, le point d'entrée où une adresse
  se résout, et ce qu'il faudrait pour mesurer l'indiscernabilité **temporelle** d'`ARB-005`.
  `T-011` en a déjà écrit le principe — latences entrelacées, plancher de bruit **mesuré** sur un
  couple témoin, et une sonde qui insère un délai et exige que la batterie rougisse. **Ne le réinvente
  pas : lis-le et complète-le.**
- **Tu ne touches aucun fichier de `src/vues/`.** Le banc est à **409 couples sur 409, 0 écart, 0
  recours** — relevé ce jour, avant ton lot, sortie jointe. Le prouver inchangé est un critère.
- **Tu n'écris pas `T-016`** (coquille applicative), ni `T-017` (notifications, décompte), ni `T-047`
  (réinitialisation de mot de passe — V-06 est à un autre lot).
- **Tu ne combles pas.** `règles/workflow_agentic.md` §4.15.
- **`cadrage/`, `mockups/`, `règles/`, `verif/references/` : lecture seule, mécaniquement.**
- **Tu ne te donnes pas ton seuil.** Si le dépôt ne peut pas passer au vert, **livre rouge en
  l'expliquant.**

### Les fichiers qu'un autre lot occupe — `T-015` tourne en parallèle

**N'y touche pas** : `src/lib/contenu/**`, `verif/contenu.mjs`, et les entrées `contenu:*` et
`test:aller-retour` de `package.json`.

Ton territoire : `src/lib/auth/**` *(hors `Marque.svelte`, qui est du gel porté)*, `src/lib/base/**`,
`base/migrations/**`, `src/routes/**`, `src/hooks.server.ts`, `src/app.d.ts`.

### `P-16` — tu installes une dépendance, et c'est le piège de ce lot

`@node-rs/argon2` 2.1.0 n'est pas installé (`package.json` porte trois dépendances : `zod`,
`drizzle-orm`, `pg`). Or `verif/preparer-copie.sh:23` **lie** `node_modules` à l'arbre principal, et
se justifie ainsi : *« le lien est sûr ici parce qu'aucun lot n'installe de dépendance »*.

**Ton `pnpm add` écrirait donc dans l'arbre de `T-015`, sous lui.** Avant toute installation :

```bash
rm node_modules && pnpm install --frozen-lockfile      # ~10 s, le magasin pnpm est sur le même tmpfs
```

Puis installe. Et **dis-le en tête de rapport**, avec la version exacte obtenue : je ne rapatrie pas
un lot qui ajoute une dépendance tant qu'une autre copie tourne.

**Et si tu dois revenir en arrière, retirer la dépendance ne suffit pas** : pnpm laisse un
`pnpm-workspace.yaml` et des pairs optionnels résolus dans le lockfile, qui font sortir
`pnpm install --frozen-lockfile` en 1. Réinitialise le lockfile depuis `HEAD`, puis réinstalle.

**Si `@node-rs/argon2` 2.1.0 n'est pas installable** — binaire natif absent pour cette plateforme,
version disparue du registre —, **arrête-toi et déclare-le**. Ne substitue pas un autre algorithme :
`STACK-TECHNIQUE.md:321` nomme Argon2id, et la substitution serait une décision fonctionnelle prise
en exécution, donc un défaut de contrat.

---

## 6 · Ne me crois pas sur parole

**Neuf affirmations que j'ai transmises se sont révélées fausses en une journée**, toutes parce que je
n'avais pas ouvert le fichier — `P-21` a été écrit contre moi. **Dans le contrat frère de ce lot,
trois de mes sept références de ligne étaient fausses au premier jet.**

**Vérifie chacune** : les 18 tables de `schema.ts` et l'absence de condensat ; les libellés de V-05 aux
lignes citées ; les valeurs de `?motif=` à `routes.md:286` ; `ATTENDU_ROUTES` à `menus.mjs:923` ; la
ligne `semence.ts:484` ; les choix de durée à `V-33:2963`.

**Les bornes qui doivent t'arrêter — si l'une se réalise, arrête-toi et déclare-la :**

1. **Si un état déclaré de V-05 exige un comportement serveur que tu ne peux pas rendre sans toucher
   le composant**, arrête-toi. Le banc est la loi, et 409 couples en dépendent.
2. **Si une maquette montre un barème de tentatives** — un nombre d'essais, une progression de délais
   —, ligne ouverte et citée, alors elle est la loi et prime sur ton barème. Je n'ai trouvé que
   `verrouiller(90)` et je ne l'ai cherché que dans V-05 et V-33.
3. **Si `RG-M16-01` « depuis une même origine » se révèle impossible à tenir honnêtement** — l'adresse
   d'origine derrière le frontal Caddy n'étant pas celle du client —, **dis-le** : c'est un vide réel,
   et non un détail d'implémentation. Compte-le, et propose sans décider.
4. **Si l'indiscernabilité temporelle de `ARB-005` t'est inaccessible**, ne la déclare pas tenue.
   `docs/routes.md:460` l'interdit explicitement tant que la batterie 6 n'existe pas, et `T-011` a eu
   raison de refuser.
5. **Si `ARB-050` est faux** — si un chemin que je n'ai pas vu fait bien lire les `*_BASE` —,
   contredis-moi avec la ligne. La mesure tranche, jamais l'autorité.

---

## 7 · Les pièges de `CLAUDE.md` §6 qui te concernent nommément

| Piège | Pourquoi il te vise |
|---|---|
| **`P-16`** | §5 ci-dessus. C'est **le** piège de ce lot |
| **`P-13`** | tu travailles dans le module même que le piège vise. Ne compose **jamais** d'URI ; le connecteur reçoit un objet |
| **`P-11`** | PostgreSQL 18 se monte sur `/var/lib/postgresql`, pas `…/data`. Si tu recrées un conteneur, tu le sais |
| **`P-2`** | une copie fraîche déclare « no tests » au lieu d'échouer. Si tu le vois, **c'est un échec** |
| **`P-5`** | deux fois : ton garde-fou de session, et la correction d'`ARB-050`. Une règle qu'aucun cas n'exerce est une règle qu'on espère |
| **`P-17`** | un accent grave dans un modèle littéral ferme le modèle. Tu écris du SQL de migration : l'erreur remonterait à cent lignes de la cause |
| **`P-21`** | §6 ci-dessus |
| **`P-22`** | si tu crées un conteneur, **nomme-le dans ton rapport**. Et ne repère jamais un processus par `pgrep` sur un motif : il se trouve lui-même, et il attrape ceux des autres copies |

**Une base de développement est debout et à jour**, recréée ce jour : projet `codicillus`, base sur
`127.0.0.1:19432`, secrets dans `.env` à la racine — que `verif/preparer-copie.sh` recopie dans ta
copie. `pnpm verif:base` rend **18/18 sondes conformes**. Les conteneurs précédents portaient un
schéma **singulier** (`compte`, `note`, `session`) étranger à `base/migrations/` : ils ont été
retirés. Si tu trouves une table au singulier, tu regardes une base qui n'est pas la nôtre.

---

## 8 · Critères de sortie

### Le but — ce que le lot doit faire descendre

```
la migration 003                    → montée, descente, remontée ; empreintes identiques
pnpm base:reversibilite             → 0
un compte se connecte               → ta commande, avec sa sortie
un mot de passe faux et un identifiant inconnu → même corps, même code, même en-têtes ; ÉCART DE TEMPS MESURÉ
une route protégée sans session     → 302 vers /connexion?motif=page-protegee&suite={chemin}
une session expirée                 → 302 vers /connexion?motif=session-expiree&suite={chemin}, puis {suite} restauré
?suite= externe                     → remplacé par /, jamais un refus
la déconnexion                      → 302 vers /, jamais une page d'erreur
le ralentissement                   → MESURÉ : le barème, les délais obtenus, la durée transmise
la durée de session                 → lue dans `parametres`, jamais codée en dur ; « se souvenir de moi » en exempte
ARB-050                             → les cinq *_BASE lues, URL_BASE retiré, deux unitaires qui l'exercent
pnpm test:unit                      → 0, tes cas inclus, chacun nommé
```

### La condition — tout le reste, INCHANGÉ

**C'est le vrai critère.** *Quatre lots de suite ont fermé leur cible en déplaçant autre chose.*

```
pnpm check                          → 0
pnpm verif:gel                      → 43 empreintes intactes
pnpm verif:jetons                   → 0
pnpm verif:inventaire               → 0
pnpm verif:demo:hors-production     → 0
pnpm verif:fraicheur                → 0
pnpm verif:maquette:app             → 409 couples · 409 conformes · 0 écart · 0 recours
pnpm verif:base                     → 18/18 sondes conformes
pnpm test:droits · test:vide · test:etats · test:a11y · test:impression → inchangés
pnpm verif:vocabulaire              → inchangé (seuils 138 / 67)
pnpm verif:menus                    → inchangé : rouge à 81, dette arbitrée ARB-047. Il doit rester
                                      rouge À 81 et sortir en 1, jamais en 2 : un code 2 signifie que
                                      ATTENDU_ROUTES a divergé, donc que tu as touché le §3 de routes.md
```

**`pnpm verif:maquette:app` prend 6 à 9 minutes et un autre lot tourne** : lance-le **une seule fois,
à la fin**. Deux lots qui jouent le banc ensemble demandent plus de deux heures d'horloge pour 368 s
de temps instrument.

---

## 9 · Livrable

Le code, ses migrations montée **et** descente, ses unitaires, et un rapport donnant :

1. **la ligne de base**, relevée avant écriture, avec la sortie réelle ;
2. **la dépendance installée**, sa version exacte, et le geste `P-16` que tu as posé ;
3. **le schéma ajouté** : chaque table, chaque colonne, et l'exigence qu'elle sert ;
4. **la mesure de l'indiscernabilité de l'échec** : corps, code, en-têtes, **et temps** — identifiant
   inconnu contre mot de passe faux, plusieurs tirages, l'écart et sa dispersion. Si un canal
   subsiste, **dis-le et compte-le** ; ne le déclare pas fermé ;
5. **le barème de ralentissement** retenu, sa source, et les délais réellement obtenus ;
6. **le tableau des redirections** de `routes.md` §5.2, chacune avec la sortie qui la prouve ;
7. **`ARB-050`** : la correction, et les deux cas qui l'exercent ;
8. **ce que ton lot rend mesurable pour la batterie 6** — §5 ;
9. **ce que le lot ne couvre pas**, et pourquoi ;
10. **la sortie réelle de chaque commande** du §8, les deux groupes ;
11. **tes écarts numérotés `É-1`, `É-2`…**

**Ne commite pas. Ne crée rien dans `docs/ecarts/` — je numérote.**

**Un rapport sans aucun écart est suspect, pas rassurant.**
