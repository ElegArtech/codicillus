<!--
	LE CORPS RÉDIGÉ DU REGISTRE OPÉRATIONNEL — VIDÉ, pour la même raison que son
	jumeau `CorpsReference.svelte`, dont l'en-tête porte le détail.

	Il transcrivait le pas-à-pas de la note de démonstration — `bkp-01.prod`, le
	compte `barman`, `pg-prod-01`, `pg-prod-02` — et n'était rendu nulle part :
	`NoteDeDemonstration.svelte` ne le monte que dans la branche `{:else}` d'une
	propriété REQUISE. Il partait néanmoins dans le paquet de `/notes/{id}`.

	Le corps d'une note réelle sort de `rendreDocument` (ADR-004).
-->
